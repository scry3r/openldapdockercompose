PHPLDAPADMIN_LDAP_HOSTS:
  - ldap-host:
    - server:
      - tls: false
      - port: 389
      - force_may: array('uidNumber','gidNumber','sambaSID')
    - login:
      - bind_id: dc=host1,dc=local
      - bind_pass: max
    - auto_number:
      - min: array('uidNumber'=>1500,'gidNumber'=>1500)
